# 工作室视觉与交互验收

日期：2026-09-22。页面：http://127.0.0.1:5173/

## 对比目标与证据

- Source visual truth：`D:/codex/home/generated_images/01a0c871-8297-7770-822a-d569c36049a3/exec-721b62d6-376e-4e47-88da-06f9b9bd6a08.png`，用户选择的第一张工作室概念图。
- Implementation：`design-qa/studio-final.png`，1487×1058 CSS 视口 / 1487×1058 PNG。源图同为 1487×1058；1:1 对比，无密度缩放，无浏览器边框。
- 源图与最终实现、白板状态截图在同一组图像输入中共同查看后评审，未用代码或路径代替图像判断。
- 状态：源图为柜前取物概念；实现主图为柜前待机。另捕获 `carrying-final.png` 的真实持物状态及 `board-final.png` 的白板旁对话状态。人物动作时刻不同，未当作静态像素差异。
- 辅助证据：`studio-before.png`、`board-before.png`、`carrying.png`、`classic.png`、`mobile-final.png`。窄屏为 390×844 CSS / PNG。
- 全图以原尺寸显示，白板、柜子、显示器、控制按钮均能逐项辨认；手与道具额外检查持物截图。因此没有对已可清楚读到的文字重复裁剪。

## 发现与修复记录

1. **P2：人物视角按钮与底栏相交。** 第一轮 `studio-before.png` 中靠近脚部的按钮进入底栏范围，人物走到工作桌时更明显。已将工作室视角按钮移到右上方。`studio-final.png` 中按钮与人物及底栏分离，完整可见。
2. **P2：柜子标签像纵向贴片，白板控件配色不合。** 第一轮标签覆盖大半文件夹，深色复选框压在浅板上。改为横向短标签，并为白板使用浅色控件。最终标签贴合文件夹下半部，复选框为清晰浅底轮廓。
3. **P1：白板站位挡住内容与点击。** `board-before.png` 中人物头身及透明点击区覆盖白板，点击复选框实际触发招呼。把站位移到白板左侧（房间宽度 63.7%），收窄人物点击区；到达后实测白板复选框可正常保存。
4. **P2：持物时双臂下垂，文件悬空。** `carrying.png` 中两手远离文件；真实骨骼测量表明原先绕前臂 X 轴产生扭转。改为 Y 轴屈肘，双手间距从 58.9 cm 改为 26.6 cm，局部道具偏移随朝向变化。`carrying-final.png` 显示双手扶住文件两侧。
5. **P2：白板旁气泡覆盖待办。** 白板站位修正后，对话仍向右展开。改为接近白板时向人物左侧展开；`board-final.png` 中对白与待办区域分开。
6. **窄屏检查：** 房间保留完整比例，场景内字较小，底部提供独立入口，按钮高度 40 px；点击“任务白板”打开完整字号的编辑清单。已实际打开并检查，无横向溢出或被遮住的持久控制。

以上 P1/P2 均在后续同尺寸渲染中复查；没有遗留的 P0/P1/P2 阻断项。

## 五项视觉检查

- **字体与排版：** 继续使用应用既有 Segoe UI / Microsoft YaHei 系统字体；海报使用 Georgia 衬线。源图手写待办改为实际 HTML 文字，属于可编辑内容的有意调整。桌面白板文字 16 px、标题 22 px，列表层级清楚；长待办截断后可在完整清单中阅读。
- **间距与布局：** 保留源图柜子—工作桌—白板—窗边四个区域及前景地面。底栏增加可操作入口与动作状态，以支撑原型体验；不复制概念图中的桌面窗口最小化/关闭按钮。人物与脚部控制不再重叠。
- **颜色：** 保留蓝灰墙、暖木地面、浅米白板及绿色点缀。底栏为半透明深绿灰，主要体验按钮为淡绿，页脚增加深色底以保证在亮地毯上的对比。
- **图像与人物：** 房间使用源图衍生的清洁背景 PNG，保持相同比例与家具构图；文件道具为生成的透明 PNG，无白底。人物是实际 VRM，和概念图中的绘制人物存在材质、面部、光照差异，这是本次接入实时人物的已知实现范围，没有使用静态人物图冒充实时角色。
- **文案与内容：** 柜子、显示器及白板使用当前真实本地记录；未把概念中的示例任务伪装成用户数据。场景按钮、管理待办、跳过动作、体验取资料、停止等文案与实际功能对应。

## 功能与运行检查

- 双向场景切换、场景偏好刷新保留、经典图标位置保留。
- 笔记未保存草稿在切换中保留；白板勾选在经典场景同样生效，跨场景撤销可恢复。
- 内容立即打开，人物独立移动；取资料完整访问柜子、工作桌、白板。
- 取资料移动中点击场景切换，确认不携带道具、不继续旧动作，记录不受影响。
- 43/43 自动测试通过；最终应用语法检查通过。
- 浏览器最后捕获的 error/warn 列表为空。一次长动作等待接口超时后通过实际页面状态确认流程已完成，不将工具等待超时记作页面故障。

## 范围与后续细化

工作室为固定视角 2.5D 背景加实时 3D 人物；路径是家具前方的固定通道，没有可绕行的 3D 家具碰撞。人物材质与环境照明融合、指尖握持细节及极小屏幕的房间细节属于后续细化。自定义 VRM、真实语音设备与原生桌面控制未在本轮扩展。

## 实施清单

- [x] 工作室背景与实时人物独立渲染。
- [x] 柜子、工作桌、白板绑定现有本地数据。
- [x] 新旧场景切换与持久化。
- [x] 有目的的走动、取物、持物、指示及取消。
- [x] 桌面、窄屏与关键交互验证。
- [x] 修复后视觉回归与错误检查。

final result: passed
